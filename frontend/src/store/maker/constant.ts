const namespace = "maker";

const fetch = `${namespace}/fetch`;
const fetchSuccess = `${namespace}/fetchSucess`;
const failed = `${namespace}/failed`;
const setLoading = `${namespace}/setLoading`;

export default {
  fetch,
  fetchSuccess,
  failed,
  namespace,
  setLoading,
};
